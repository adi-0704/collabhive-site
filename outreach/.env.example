# Copy to .env locally for testing, OR set as GitHub Actions secrets.
# NEVER commit .env. Your Gmail app password must live ONLY in GitHub Secrets
# (OUTREACH_EMAIL_PASS) or in the local .env — never in this file or the repo.

OUTREACH_EMAIL_USER=collabhive.in@gmail.com
OUTREACH_EMAIL_PASS=your_gmail_app_password_here
